// Simulasi waktu loading (sesuaikan dengan kebutuhan)
const loadingTime = 2000; // 2 detik

// Setelah loading selesai, redirect ke halaman lain
setTimeout(() => {
    window.location.href = "halaman_tujuan.html"; // Ganti dengan URL yang ingin dituju
}, loadingTime);
